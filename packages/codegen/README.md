# @inspect/codegen

Generates executable test scripts from recorded workflows. Converts user interaction recordings into reusable Playwright test code.

## Usage

```ts
import { CodeGenerator } from "@inspect/codegen/code-generator.js";
```

## Key Exports

- `CodeGenerator` — transforms workflow recordings into test scripts
- `TemplateEngine` — code template rendering
