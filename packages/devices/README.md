# @inspect/devices

Device detection and configuration. Manages target devices, viewport profiles, and device-specific test configurations.

## Usage

```ts
import { DeviceManager } from "@inspect/devices/device-manager.js";
```

## Key Exports

- `DeviceManager` — device profile management
- `DeviceProfile` — schema for device configuration (viewport, user-agent, etc.)
