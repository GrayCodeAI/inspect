# @inspect/resilience

Resilience testing utilities. Validates application behavior under stress, recovery from failures, and graceful degradation patterns.

## Usage

```ts
import { ResilienceTester } from "@inspect/resilience/resilience-tester.js";
```

## Key Exports

- `ResilienceTester` — runs resilience test scenarios
- `RecoveryCheck` — validates application recovery behavior
