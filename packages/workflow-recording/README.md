# @inspect/workflow-recording

Records user interactions and generates test scripts. Captures browser interactions as structured workflows that can be replayed or converted to code.

## Usage

```ts
import { WorkflowRecorder } from "@inspect/workflow-recording/workflow-recorder.js";
```

## Key Exports

- `WorkflowRecorder` — captures user interactions as workflows
- `Workflow` — schema for recorded interaction sequences
