# @inspect/plugin-marketplace

Plugin system foundation for inspect. Enables extensibility through a plugin marketplace architecture for custom test types and integrations.

## Usage

```ts
import { PluginRegistry } from "@inspect/plugin-marketplace/plugin-registry.js";
```

## Key Exports

- `PluginRegistry` — manages plugin discovery and loading
- `PluginManifest` — schema for plugin metadata
