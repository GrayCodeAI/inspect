# @inspect/crawler

Web crawler for automatic test discovery. Navigates applications to find pages, forms, and interactive elements that should be tested.

## Usage

```ts
import { Crawler } from "@inspect/crawler/crawler.js";
```

## Key Exports

- `Crawler` — automated site navigation and page discovery
- `CrawlResult` — schema for crawled pages and elements
