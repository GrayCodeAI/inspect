# @inspect/visual

Visual diffing and screenshot comparison. Detects visual regressions by comparing baseline and current screenshots pixel-by-pixel.

## Usage

```ts
import { VisualDiff } from "@inspect/visual/visual-diff.js";
```

## Key Exports

- `VisualDiff` — screenshot comparison engine
- `DiffResult` — schema for visual diff findings
