# @inspect/cli-context

CLI context management for inspect. Provides command-line argument parsing, configuration loading, and runtime context for the inspect CLI.

## Usage

```ts
import { CliContext } from "@inspect/cli-context/cli-context.js";
```

## Key Exports

- `CliContext` — manages CLI runtime state and configuration
- `parseArgs` — command-line argument parser
