# @inspect/session

Browser session management. Handles test session lifecycle, page management, and session-level recording with Playwright and rrweb.

## Usage

```ts
import { SessionManager } from "@inspect/session/session-manager.js";
```

## Key Exports

- `SessionManager` — manages browser test sessions
- `Session` — schema for a single test session
