# @inspect/agent-governance

Policy enforcement and guardrails for autonomous agent behavior. Ensures agents operate within defined safety and scope boundaries.

## Usage

```ts
import { PolicyEngine } from "@inspect/agent-governance/policy-engine.js";
```

## Key Exports

- `PolicyEngine` — evaluates agent actions against governance rules
- `GovernancePolicy` — schema for defining agent constraints
