# @inspect/sandbox

Sandboxed test execution environment. Runs tests in isolated contexts to prevent side effects and ensure reproducible results.

## Usage

```ts
import { Sandbox } from "@inspect/sandbox/sandbox.js";
```

## Key Exports

- `Sandbox` — isolated test execution environment
- `SandboxConfig` — sandbox configuration schema
