# @inspect/mocking

Request mocking and response interception for browser tests. Enables deterministic testing by stubbing network responses and API calls.

## Usage

```ts
import { MockEngine } from "@inspect/mocking/mock-engine.js";
```

## Key Exports

- `MockEngine` — manages request/response mocking
- `MockRule` — schema for defining mock intercept rules
