# @inspect/core

Core inspect functionality. Entry point that wires together the orchestrator, git integration, and device management into a cohesive testing pipeline.

## Usage

```ts
import { Inspect } from "@inspect/core/inspect.js";
```

## Key Exports

- `Inspect` — main entry point for running inspect
- `RunConfig` — test run configuration schema
