# @inspect/workflow

Workflow orchestration for complex test scenarios. Defines, sequences, and executes multi-step test workflows with quality integration.

## Usage

```ts
import { WorkflowEngine } from "@inspect/workflow/workflow-engine.js";
```

## Key Exports

- `WorkflowEngine` — executes multi-step test workflows
- `WorkflowDefinition` — schema for workflow specifications
