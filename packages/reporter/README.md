# @inspect/reporter

Test result reporting and formatting. Generates human-readable reports, summaries, and exportable formats from test execution results.

## Usage

```ts
import { Reporter } from "@inspect/reporter/reporter.js";
```

## Key Exports

- `Reporter` — formats and outputs test results
- `ReportFormat` — supported output format types
