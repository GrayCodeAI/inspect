# @inspect/credentials

Secure credential management for test sessions. Handles storage and injection of authentication credentials, API keys, and secrets.

## Usage

```ts
import { Credentials } from "@inspect/credentials/credentials.js";
```

## Key Exports

- `Credentials` — secure credential storage and retrieval
- `CredentialStore` — encrypted credential backend
