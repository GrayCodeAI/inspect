# @inspect/services

Shared service definitions for inspect. Provides common Effect services used across packages for configuration, lifecycle, and infrastructure.

## Usage

```ts
import { ConfigService } from "@inspect/services/config-service.js";
```

## Key Exports

- `ConfigService` — application configuration service
- `LifecycleManager` — service lifecycle management
