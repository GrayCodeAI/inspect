# @inspect/network

Network monitoring and interception for browser tests. Captures HTTP traffic, analyzes requests/responses, and detects network-related issues.

## Usage

```ts
import { NetworkMonitor } from "@inspect/network/network-monitor.js";
```

## Key Exports

- `NetworkMonitor` — captures and analyzes network traffic
- `NetworkRequest` — schema for HTTP request/response data
