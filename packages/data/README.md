# @inspect/data

Data layer for inspect. Provides typed data access, validation schemas, and persistence utilities for test results and configuration.

## Usage

```ts
import { DataStore } from "@inspect/data/data-store.js";
```

## Key Exports

- `DataStore` — typed data persistence layer
- `ValidationSchema` — Zod-based data validation schemas
