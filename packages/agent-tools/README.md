# @inspect/agent-tools

Tool definitions for AI agents. Provides browser interaction, navigation, and inspection capabilities that agents can invoke during test execution.

## Usage

```ts
import { BrowserTool } from "@inspect/agent-tools/browser-tool.js";
```

## Key Exports

- `BrowserTool` — browser interaction tool for agents
- `ToolRegistry` — manages available agent tools and their schemas
