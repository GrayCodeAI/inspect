# @inspect/quality

Quality aggregation layer. Combines accessibility, Lighthouse, chaos, security, resilience, and mocking checks into unified quality reports.

## Usage

```ts
import { QualityReport } from "@inspect/quality/quality-report.js";
```

## Key Exports

- `QualityReport` — aggregated quality assessment
- `QualityChecker` — runs all quality checks against a page
