# @inspect/environments

Ephemeral test environment provisioning. Spins up isolated Docker-based environments for reproducible test execution.

## Usage

```ts
import { EnvironmentManager } from "@inspect/environments/environment-manager.js";
```

## Key Exports

- `EnvironmentManager` — Docker-based environment lifecycle
- `EnvironmentConfig` — schema for environment specification
